# Build Configuration
# These values can be overridden by environment variables (e.g., in CI/CD)

# Linux kernel version to build
KERNEL_VERSION=6.17

# BusyBox version to build
BUSYBOX_VERSION=1.36.1
